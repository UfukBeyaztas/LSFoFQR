## 📦 Installation

You can install the development version of **LSFoFQR** from GitHub:

```r
install.packages("remotes")
remotes::install_github("UfukBeyaztas/LSFoFQR")

Then load the package:

library(LSFoFQR)

User manual included: the package is accompanied by the manual file LSFoFQR_0.1.0.pdf
